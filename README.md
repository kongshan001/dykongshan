# DY Kong Shan - 子域名导航中心

一个简洁、高效的子域名导航应用，基于 uni-app 开发，一套代码同时支持 H5 网页和微信小程序。

## 功能特性

### 核心功能
- 子域名导航 - 快速访问各个子域名服务
- 实时搜索 - 支持按标题和描述搜索
- 分类筛选 - 按类别组织子域名
- 点击统计 - 记录每个子域名的访问次数

### 多端支持
- H5 网页版
- 微信小程序

## 技术栈

- **框架**: uni-app + Vue 3 (Composition API)
- **构建工具**: Vite
- **状态管理**: Pinia
- **样式**: SCSS

## 快速开始

### 环境要求
- Node.js >= 16.0.0
- npm >= 7.0.0

### 安装依赖
```bash
npm install
```

### 开发模式

```bash
# H5 网页开发
npm run dev:h5

# 微信小程序开发
npm run dev:mp-weixin
```

### 构建生产版本

```bash
# 构建 H5 网页
npm run build:h5

# 构建微信小程序
npm run build:mp-weixin
```

## 微信小程序使用说明

### 1. 构建小程序
```bash
npm run build:mp-weixin
```

### 2. 导入开发者工具
1. 下载并安装 [微信开发者工具](https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html)
2. 打开微信开发者工具
3. 导入项目，选择 `dist/build/mp-weixin` 目录
4. 填写 AppID（可在微信公众平台获取）

### 3. 配置 AppID
编辑 `src/manifest.json`，填写你的小程序 AppID：
```json
{
  "mp-weixin": {
    "appid": "你的小程序AppID"
  }
}
```

## 项目结构

```
dykongshan_page/
├── dist/
│   └── build/
│       ├── h5/                    # H5 构建产物
│       └── mp-weixin/             # 小程序构建产物
├── src/
│   ├── components/                # 公共组件
│   ├── data/
│   │   └── links.json             # 子域名配置
│   ├── pages/
│   │   └── index/
│   │       └── index.vue          # 首页
│   ├── static/                    # 静态资源
│   ├── stores/
│   │   └── links.js               # Pinia 状态管理
│   ├── App.vue                    # 根组件
│   ├── main.js                    # 入口文件
│   ├── manifest.json              # uni-app 配置
│   ├── pages.json                 # 页面路由配置
│   └── uni.scss                   # 全局样式变量
├── package.json
├── vite.config.js
└── README.md
```

## 配置管理

### 添加新子域名

编辑 `src/data/links.json` 文件：

```json
{
  "links": [
    {
      "id": "new-link",
      "title": "新链接标题",
      "url": "https://xxx.dykongshan.com",
      "description": "链接描述",
      "categoryId": "dev",
      "icon": "🔗",
      "clickCount": 0,
      "createdAt": "2024-01-15"
    }
  ]
}
```

### 添加新分类

在 `src/data/links.json` 的 `categories` 数组中添加：

```json
{
  "categories": [
    {
      "id": "new-category",
      "name": "新分类名称",
      "icon": "📁"
    }
  ]
}
```

## 部署

### H5 部署 (Vercel)

1. 构建项目
   ```bash
   npm run build:h5
   ```

2. 部署 `dist/build/h5` 目录到 Vercel

### 小程序发布

1. 在微信开发者工具中打开项目
2. 点击「上传」按钮
3. 在微信公众平台提交审核

## 常用命令

| 命令 | 说明 |
|------|------|
| `npm run dev:h5` | H5 开发模式 |
| `npm run dev:mp-weixin` | 小程序开发模式 |
| `npm run build:h5` | 构建 H5 |
| `npm run build:mp-weixin` | 构建小程序 |

## 在线访问

- **H5 网页**: https://dykongshan.com
- **GitHub**: https://github.com/kongshan001/dykongshan

## 许可证

MIT License
