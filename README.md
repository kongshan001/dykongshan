# DY Kong Shan - 子域名导航中心

一个简洁、高效的子域名导航网站，支持快速跳转、搜索筛选、分类管理和点击统计。

## ✨ 功能特性

### 核心功能
- 🎯 **子域名导航** - 快速访问各个子域名服务
- 🔍 **实时搜索** - 支持按标题和描述搜索，不区分大小写
- 📂 **分类筛选** - 按类别组织子域名，快速筛选
- 📊 **点击统计** - 记录每个子域名的访问次数
- 🌓 **主题切换** - 支持明亮/暗黑主题切换
- 📱 **响应式设计** - 完美适配移动端、平板和桌面端

### 管理功能
- 📝 **JSON配置** - 通过配置文件管理所有子域名
- 🏷️ **自定义分类** - 灵活配置分类和图标
- 🔗 **链接管理** - 轻松添加、编辑子域名信息

## 🛠️ 技术栈

- **框架**: Vue 3 (Composition API)
- **构建工具**: Vite
- **状态管理**: Pinia
- **样式方案**: Tailwind CSS
- **UI组件**: Element Plus
- **测试框架**: Vitest + @vue/test-utils
- **部署平台**: Vercel

## 🚀 快速开始

### 环境要求
- Node.js >= 16.0.0
- npm >= 7.0.0

### 安装依赖
```bash
npm install
```

### 本地开发
```bash
npm run dev
```

访问 http://localhost:5173 查看项目

### 构建生产版本
```bash
npm run build
```

### 预览生产构建
```bash
npm run preview
```

## 🧪 测试

项目包含完整的单元测试，确保代码质量和功能稳定性。

### 测试框架
- **Vitest** - 快速的单元测试框架
- **@vue/test-utils** - Vue组件测试工具
- **@testing-library/vue** - 用户交互测试
- **jsdom** - 测试环境模拟

### 测试覆盖

#### 测试文件
- `src/tests/links.spec.js` - Links Store 测试（14个用例）
- `src/tests/SearchBar.spec.js` - 搜索框组件测试（8个用例）
- `src/tests/LinkCard.spec.js` - 链接卡片组件测试（10个用例）
- `src/tests/CategoryFilter.spec.js` - 分类筛选组件测试（4个用例）

**总计**: 36个测试用例，100%通过率 ✅

### 运行测试

#### 运行所有测试（监听模式）
```bash
npm run test
```

#### 运行所有测试（单次）
```bash
npm run test:run
```

#### 运行测试UI
```bash
npm run test:ui
```

打开浏览器查看测试界面，支持可视化测试结果和调试。

#### 运行测试覆盖率
```bash
npm run test:coverage
```

生成详细的测试覆盖率报告。

### 测试覆盖的功能

#### 1. 搜索功能
- ✅ 按标题搜索子域名
- ✅ 按描述搜索子域名
- ✅ 不区分大小写搜索
- ✅ 空搜索返回所有结果
- ✅ 实时输入响应

#### 2. 分类筛选功能
- ✅ 按分类ID筛选链接
- ✅ 点击分类按钮切换分类
- ✅ 点击"全部"显示所有链接
- ✅ 选中状态高亮显示

#### 3. 点击统计功能
- ✅ 增加点击次数
- ✅ 保存统计数据到localStorage
- ✅ 从localStorage加载历史统计
- ✅ 显示点击次数

#### 4. 组件交互
- ✅ 搜索框输入更新
- ✅ 清除搜索按钮
- ✅ 分类按钮点击
- ✅ 链接卡片点击
- ✅ 阻止默认链接行为

#### 5. 状态管理
- ✅ Pinia Store全局状态
- ✅ 搜索状态同步
- ✅ 分类状态同步
- ✅ 响应式数据更新

#### 6. 组件渲染
- ✅ 搜索框正确渲染
- ✅ 分类按钮正确渲染
- ✅ 链接卡片正确渲染
- ✅ 样式类正确应用

### 测试Mock

项目使用Mock来隔离外部依赖，确保测试的稳定性和可重复性。

#### Mock的对象
- `localStorage` - 本地存储
- `window.matchMedia` - 媒体查询
- `ResizeObserver` - 尺寸观察器
- `links.json` - 测试数据

#### Mock数据示例
```javascript
{
  categories: [
    { id: 'dev', name: '开发工具', icon: '⚙️' },
    { id: 'docs', name: '文档中心', icon: '📚' }
  ],
  links: [
    {
      id: 'cicd',
      title: 'CI/CD Action',
      url: 'https://cicdaction.dykongshan.com',
      description: '持续集成与部署工具',
      categoryId: 'dev',
      icon: '🚀',
      clickCount: 0,
      createdAt: '2024-01-01'
    }
  ]
}
```

### 测试最佳实践

1. **单一职责** - 每个测试用例只测试一个功能点
2. **清晰命名** - 使用`应该 + 期望结果`的命名方式
3. **独立测试** - 每个测试相互独立，不依赖执行顺序
4. **边界测试** - 测试正常流程和边界情况
5. **Mock隔离** - 使用Mock隔离外部依赖

### 添加新测试

1. 在对应的 `.spec.js` 文件中添加测试用例
2. 使用 `describe` 分组相关测试
3. 使用 `beforeEach` 初始化测试环境
4. 运行测试验证结果

示例：
```javascript
describe('新功能测试', () => {
  it('应该正确执行新功能', () => {
    // 准备
    const wrapper = mount(Component)
    
    // 执行
    await wrapper.trigger('click')
    
    // 断言
    expect(wrapper.vm.isActive).toBe(true)
  })
})
```

### 测试配置

测试配置文件：`vitest.config.js`

```javascript
export default defineConfig({
  plugins: [vue()],
  test: {
    globals: true,           // 使用全局API
    environment: 'jsdom',     // 测试环境
    setupFiles: ['./src/tests/setup.js'], // 测试设置
    coverage: {
      provider: 'v8',        // 覆盖率提供者
      reporter: ['text', 'json', 'html'],
      exclude: ['node_modules/', 'src/tests/']
    }
  }
})
```

## 📁 项目结构

```
dykongshan_page/
├── public/                     # 静态资源
│   └── vite.svg
├── src/
│   ├── assets/
│   │   └── styles/
│   │       └── global.css      # 全局样式
│   ├── components/              # Vue组件
│   │   ├── Header.vue          # 页面头部
│   │   ├── ThemeToggle.vue    # 主题切换
│   │   ├── SearchBar.vue      # 搜索框
│   │   ├── CategoryFilter.vue # 分类筛选
│   │   └── LinkCard.vue       # 链接卡片
│   ├── composables/            # 组合式函数
│   │   ├── useTheme.js        # 主题管理
│   │   ├── useLinks.js        # 链接管理（已废弃，使用Store）
│   │   └── useStats.js        # 统计管理
│   ├── data/                   # 数据文件
│   │   └── links.json         # 子域名配置
│   ├── stores/                 # Pinia Store
│   │   └── links.js           # 链接状态管理
│   ├── tests/                  # 测试文件
│   │   ├── setup.js           # 测试环境设置
│   │   ├── links.spec.js      # Store测试
│   │   ├── SearchBar.spec.js  # 搜索框测试
│   │   ├── LinkCard.spec.js   # 链接卡片测试
│   │   └── CategoryFilter.spec.js # 分类筛选测试
│   ├── App.vue                 # 根组件
│   └── main.js                 # 入口文件
├── .gitignore
├── index.html
├── package.json
├── tailwind.config.js          # Tailwind配置
├── postcss.config.js           # PostCSS配置
├── vite.config.js              # Vite配置
├── vitest.config.js            # 测试配置
└── vercel.json                # Vercel部署配置
```

## ⚙️ 配置管理

### 添加新子域名

编辑 `src/data/links.json` 文件，在 `links` 数组中添加新链接：

```json
{
  "id": "new-link",
  "title": "新链接标题",
  "url": "https://xxx.dykongshan.com",
  "description": "链接描述",
  "categoryId": "dev",
  "icon": "🔗",
  "clickCount": 0,
  "createdAt": "2024-01-15"
}
```

### 添加新分类

在 `src/data/links.json` 文件的 `categories` 数组中添加新分类：

```json
{
  "id": "new-category",
  "name": "新分类名称",
  "icon": "📁"
}
```

### 修改网站信息

编辑 `src/data/links.json` 文件的 `siteInfo` 对象：

```json
{
  "siteInfo": {
    "title": "网站标题",
    "description": "网站描述",
    "logo": "/logo.png"
  }
}
```

## 🚢 部署

### Vercel部署

1. **推送代码到GitHub**
   ```bash
   git add .
   git commit -m "提交信息"
   git push
   ```

2. **连接Vercel**
   - 访问 https://vercel.com 并登录
   - 点击 "Add New Project"
   - 导入 GitHub 仓库 `kongshan001/dykongshan`
   - 点击 "Deploy"

3. **配置自定义域名**
   - 进入项目设置 > Domains
   - 添加域名 `dykongshan.com`
   - 配置DNS记录：
     ```
     Type: A
     Name: @
     Value: 76.76.21.21

     Type: CNAME
     Name: www
     Value: 1673adfa5c1af99a.vercel-dns-017.com
     ```
   - 等待SSL证书自动生成

### 自动部署

推送代码到 `main` 分支后，Vercel会自动触发部署，通常需要1-2分钟。

## 📊 测试统计

```
Test Files: 4 passed (4)
Tests: 36 passed (36)
Pass Rate: 100% ✅
Duration: ~8s
```

## 🔧 常用命令

```bash
# 开发
npm run dev              # 启动开发服务器

# 构建
npm run build            # 构建生产版本
npm run preview          # 预览生产构建

# 测试
npm run test             # 运行测试（监听模式）
npm run test:run         # 运行测试（单次）
npm run test:ui          # 运行测试UI
npm run test:coverage    # 运行测试覆盖率

# Git
git add .               # 添加所有文件
git commit -m "message" # 提交
git push                # 推送到远程
```

## 📖 文档

- [技术方案](./技术方案.md) - 详细的技术方案和架构设计
- [项目推进记录](./项目推进记录.md) - 项目开发过程和问题记录
- [测试文档](./测试文档.md) - 完整的测试文档
- [需求文档](./需求.md) - 原始需求文档

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 许可证

MIT License

## 🌐 在线访问

- **网站**: https://dykongshan.com
- **GitHub**: https://github.com/kongshan001/dykongshan

---

**版本**: v1.0.0
**最后更新**: 2024-01-15
