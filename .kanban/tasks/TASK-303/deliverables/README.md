# 旅行景点人流量预测

## 概述
旅行景点人流量预测 功能模块，基于 React + FastAPI 技术栈实现。

## 技术架构
- 前端: React 18 + TypeScript
- 后端: FastAPI + SQLite
- 部署: Docker

## 使用方式
```bash
cd backend && pip install -r requirements.txt && uvicorn main:app
cd frontend && npm install && npm start
```
